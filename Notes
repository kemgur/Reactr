rendering: guncelleme olayi

2) npm uzerinden react kurma:
mac'ler icin "sudo" ekleriz
   sudo npm install create-react-app
   cd destop
   create-react-app ornek1_giris
   cd my-react-app
   npm start

3) echo "# Reactr" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/kemgur/Reactr.git
git push -u origin main

4) node_modulus paylasilan projelerde genelde
 yer almaz sonradan indirmek gerekebilir
  npm install

5)  package.json:  projelerdeki pom.xml gibidir.
Proje yonetim merkezidir.

6) public kisminda index.html yer alir.

7) <div> start tag
   </div> end tag
   <App/> self closing tag

8) react bootstrap kutuphanesini terminal ile yukleme
npm install react-bootstrap bootstrap@4.6.0

Proje icerisine react bootstrap kutuphanesini eklemek icin terminale
npm install react-bootstrap --save
yazdiginizda proje icerisindeki package.json kutuphanesine eklenir

Ayrica App.js'in icerisine import etmek gerekiyor. 
import 'bootstrap/dist/css/bootstrap.min.css';

9) navbar: logo ekleme kismidir

10) npm install react-router ile indirilir. Bu single pagetir.
    React router sayfalar arasi gecisi saglar.

11) {/* shortcut add varsa h1 'i goster, demis olduk' */}
        {ad && (
          <h1>Merhaba {ad} {soyad}</h1>  
          )}
 12) Sayfalar arasinda yonlendirmelerde Next.js kullanilabilir. 
     Ama biz day08 deki projede React-Router-dom kullanacagiz.         
     npm install react-router-dom ile projeye indirecegiz 
    
 13) {/* A <Switch> looks through its children <Route>s and
            renders the first one that matches the current URL. */}
      {/* Bir <Switch>, <Route>s alt öğelerine bakar ve
            geçerli URL ile eşleşen ilkini oluşturur. */}

  14) <Router></Router> Nedir?
      Local'de yonlendirmeyi saglar,Default component'leri vardir, 
      Bu componentler, Route, Link, Switch'tir.             